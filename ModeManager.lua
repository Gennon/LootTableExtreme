-- LootTableExtreme Mode Manager
-- Handles settings window toggling

-- Toggle settings window (formerly called "mode toggle")
function LootTableExtreme:ToggleMode()
    if self.ToggleSettings then
        self:ToggleSettings()
    end
end

